'use strict'

const shoppingList = ['Pane', 'Latte', 'Gelato', 'Fettine di vitello', 'Vino', 'Biscotti'];

let count = 0;

while (count < shoppingList.length) {
    console.log(shoppingList[count]);
    count++;
}